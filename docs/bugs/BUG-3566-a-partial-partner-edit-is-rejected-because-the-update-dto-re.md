---
ID: BUG-3566
aliases: [BUG-3566]
Title: A partial partner edit is rejected because the update DTO requires every create field
Status: FIXED
Severity: MEDIUM
Priority: P2
Type: BUG
Source: QA_RUN
DetectedDate: 2026-09-25
DetectedInSha: 10d5d148
AffectedModules: [services/api/src/modules/partners]
OwnerAgent: architect
ArchitectDisposition: DONE
QAReport: docs/tasks/TASK-0032-streams/QA-summary.md
RegressionId: REG-601
RelatedBacklogItem:
RelatedDecision:
RelatedImplementation: TASK-0032
CreatedAt: 2026-09-25
UpdatedAt: 2026-09-25
ResolvedAt: 2026-09-25
---

# BUG-3566 — A partial partner edit is rejected because the update DTO requires every create field

## Summary

Updating a single field of a partner through the API is refused with a validation error, because `UpdatePartnerDto` inherits every required field of `CreatePartnerDto` instead of making them optional like every sibling update DTO.

## Expected Behavior

A PATCH carrying only the changed fields succeeds; business rules are applied to the merged record.

## Actual Behavior

A partial PATCH returns 400 for the missing required create fields. The admin UI resends every editable field, so it does not hit this, but any API client does.

## Reproduction

1. As SUPER_ADMIN, `PATCH /api/platform-runtime/partners/<id>` with `{ "values": { "phone": "+1 555 0100" } }`.
2. 400 naming required fields.

## Evidence

- WP-08 live CRUD harness, 2026-09-25, finding 3.
- `services/api/src/modules/partners/dto/partner.dto.ts:79` `export class UpdatePartnerDto extends CreatePartnerDto {}`.

## Root Cause

The update DTO reuses the create DTO wholesale.

## Impact

Integrations and scripts cannot partially update partners; the UI works by always sending the full record.

## Affected Areas

partners DTOs and service; platform-runtime partners update.

## Proposed Resolution

Make every update field optional; apply the partner-type policy (BUG-3549) to the merged record. Assigned to TASK-0032 WP-04.

## Acceptance Criteria

A one-field PATCH succeeds; a PATCH that would leave a company partner without its required fields is refused.

## Regression Coverage

REG-601 (`services/api/src/modules/partners/dto/partner-update-partial.spec.ts`
— a single-field body, a tax-id-only body and an empty body all validate
cleanly, an invalid/undeclared field is still rejected;
`services/api/src/modules/partners/partners-partial-update.spec.ts` — a
single-field patch writes only that column, does not silently reset `status`
to `DRAFT`, and the type-policy check runs against the record as it would read
after the patch). Proven to fail against the pre-fix code: 3 of 6 DTO-level
cases fail when `UpdatePartnerDto` is reverted to `extends CreatePartnerDto
{}`.

## Dependencies

[[BUG-3549]]

## Related Items

[[BUG-3549]], [[BUG-3550]], [[TASK-0032]]

## Resolution

Fixed by commit `eb172564` on `agent/pah-wp04-partners` (TASK-0032 WP-04,
merged as `7f134e70`): `UpdatePartnerDto` no longer extends `CreatePartnerDto`
wholesale — every field is optional, and `PartnersService.update()` applies
the BUG-3549 type-identity policy to the record as it would read *after* the
patch, so a patch that would leave a COMPANY partner without a company name is
still refused.

## QA Retest

Verified by TASK-0032 WP-09 live QA ("Partners … Partial update (BUG-3566),
type policy on the merged record … Pass" — see
`docs/tasks/TASK-0032-streams/QA-summary.md`) and the passing
`partner-update-partial.spec.ts` / `partners-partial-update.spec.ts`.

## History

- 2026-09-25 — created from qa run at `c6fb718d`.
- 2026-09-25 — fixed at `eb172564` (WP-04); verified by WP-09 live QA;
  Architect disposition DONE.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-backlog.mjs; edit the frontmatter, not this block -->

## Related

- Modules — [[partners]]
- Regression — REG-601 (see the regression register)

<!-- GRAPH:END -->
