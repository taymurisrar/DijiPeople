---
ID: BUG-3500
aliases: [BUG-3500]
Title: Every seeded email template is active with placeholder body text
Status: FIXED
Severity: HIGH
Priority: P1
Type: BUG
Source: USER_REPORT
DetectedDate: 2026-09-13
DetectedInSha: df0f84f1
AffectedModules: [notifications, apps/web]
OwnerAgent: architect
ArchitectDisposition: FIX_NOW
QAReport: 
RegressionId: REG-505
RelatedBacklogItem: [ITEM-0194]
RelatedDecision: docs/decisions/ADR-0015-production-retires-sink-email-providers.md
RelatedImplementation: [docs/plans/EXECPLAN-0048-email-template-default-copy-and-visual-editor.md, services/api/src/modules/notifications/system-email-templates.copy.ts, services/api/src/modules/notifications/notification-events.catalog.ts, services/api/prisma/seed-config.ts, services/api/src/modules/notifications/notifications.repository.ts]
CreatedAt: 2026-09-13
UpdatedAt: 2026-09-13
ResolvedAt:
---

# BUG-3500 — Every seeded email template is active with placeholder body text

## Summary

The system email templates a tenant sees are all ACTIVE. Most of them have a
body that literally reads "This is a system placeholder email template.
Configure tenant-specific content before production sending." That is what
employees would receive for events such as leave approval the moment real
delivery is switched on.

Templates are also ACTIVE for events nothing emits (`AUTH_OTP`,
`PAYROLL_PROCESSED`). The placeholders are regenerated by the configuration seed
on every run.

## Expected Behavior

Every system email template that ships ACTIVE carries real, professional default
copy for its event: a meaningful subject, HTML and text bodies, and the
variables the event actually supplies. Owner decision 2026-09-13: the Architect
drafts that copy per event, and it ships ACTIVE together with the retirement of
sink providers (ADR-0015, [[BUG-3501]]).

## Actual Behavior

- `/settings/notifications/templates` lists 11 email templates, all ACTIVE.
- Opening "Leave approved email" shows the placeholder text in both the HTML and Text bodies:
  - Body: "This is a system placeholder email template. Configure tenant-specific content before production sending."
  - Description: "System placeholder template for Leave approved."
- ACTIVE templates also exist for `AUTH_OTP` and `PAYROLL_PROCESSED`, events with no emitter.

## Reproduction

1. Sign in to `https://dijipeople-demo.ws.dijipeople.com` (demo tenant) as the workspace owner.
2. Open Settings → Notifications → Email Templates (`/settings/notifications/templates`).
3. Observe that all 11 templates are ACTIVE.
4. Open "Leave approved email" and read the HTML Template, Text Template and Description.
5. Observe the `AUTH_OTP` and `PAYROLL_PROCESSED` templates in the list, both ACTIVE.

## Evidence

Browser QA on the live demo tenant at `df0f84f1`. Code references are at the
worktree HEAD.

**The placeholder is generated in the notification catalog.**
`services/api/src/modules/notifications/notification-events.catalog.ts:554-557`
builds `SYSTEM_EMAIL_TEMPLATE_PLACEHOLDERS` from every catalog event that has a
`systemTemplateKey`. `createSystemTemplateSeed` writes real copy for only three
events: `AUTH_ACCOUNT_ACTIVATION`, `AUTH_PASSWORD_RESET` and
`SUPPORT_CASE_UPDATE` (lines 559-738). Every other event falls through to the
generic return at lines 740-759, which sets:
- `description` "System placeholder template for …" (line 745)
- the placeholder HTML and text bodies (lines 747-750)
- `status: EmailTemplateStatus.ACTIVE` (line 756)

**The seed overwrites existing rows on every run.**
`services/api/prisma/seed-config.ts:876-912` upserts each of those seeds. Its
`update` branch (lines 900-911) rewrites `htmlTemplate`, `textTemplate`,
`description` and `status` on existing rows. Re-running the seed therefore
restores the placeholder and ACTIVE status on every system template.

**The seed is part of the release script.**
`services/api/package.json:34` defines `release` as migrate → `seed:config` →
…, and `render.yaml:38` declares `preDeployCommand: npm --workspace api run release`.
The live Render service configuration has previously been found to diverge from
`render.yaml`, so this record does not assert that every production deploy ran
the seed. The declared intent is that it does.

## Root Cause

The generic fallback in `createSystemTemplateSeed`
(`notification-events.catalog.ts:740-759`) seeds placeholder copy with
`status: ACTIVE` for every event that has a `systemTemplateKey` but no
hand-written template. `seed-config.ts:876-912` applies that catalog with an
upsert whose `update` branch re-imposes the placeholder bodies and ACTIVE status
on every run. The catalog is the source of truth, so real copy has to be written
there. Editing the rows would be reverted by the next seed.

## Impact

- **Go-live blocker.** When tenant email is delivered for real, employees of every tenant receive placeholder text for leave, approval and similar events.
- **Current state:** the demo tenant's only provider is CONSOLE ([[BUG-3501]]), so these emails are not reaching inboxes today. That is exactly why the defect has been invisible.
- **Why it cannot wait:** the owner's decision to retire CONSOLE providers makes real delivery imminent, so the two must ship together.
- ACTIVE templates for events that are never emitted mislead administrators about what the product sends.

## Affected Areas

- API `notifications` module: the event catalog's system template seeds.
- `services/api/prisma/seed-config.ts` (system email template seeding).
- Tenant settings: Notifications → Email Templates list and editor.
- Every tenant's outbound notification email.

## Proposed Resolution

- Write real default copy in `notification-events.catalog.ts` for every event with a `systemTemplateKey`, using only the variables each event's emitter actually supplies. Take `SUPPORT_CASE_UPDATE` (lines 700-738) as the pattern.
- Remove the placeholder fallback, so an event without authored copy cannot silently seed a placeholder.
- Decide per event whether one with no emitter keeps a template at all. Align with the catalog hygiene work rather than seeding ACTIVE templates for dead events.
- Ship together with [[BUG-3501]]'s provider change, per ADR-0015.

This is copy authoring plus a catalog change, with no schema change; no ExecPlan
needed.

## Acceptance Criteria

- No system email template, in the catalog or on a freshly seeded database, contains the text "system placeholder".
- Every ACTIVE system template has a subject, HTML body and text body specific to its event.
- Every `{{variable}}` used in a template is supplied by that event's emitter.
- Running `seed:config` twice leaves the authored copy in place.
- No ACTIVE system template exists for an event the catalog marks unavailable or that has no emitter.
- On the demo tenant, "Leave approved email" shows the authored copy after deploy.

## Regression Coverage

QA scenario QA-SETTINGS-027.

- REG-505: `services/api/src/modules/notifications/system-email-templates.spec.ts`
  — no placeholder wording in any template field; tokens used equal variables
  declared, per template; the exact ACTIVE/DRAFT map; the catalog throws with
  missing copy; the sanitiser keeps tokens and links and strips unsafe markup.
  Mutation: the placeholder sentence in the payslip copy fails it.
- REG-506: `services/api/src/modules/notifications/system-email-template-emitters.spec.ts`
  — every ACTIVE template's declared variables are passed at every emitter call
  site. Mutation: `{{logoUrl}}` added to the reset copy fails it.
- REG-509: the `planSystemTemplateWrite` seed guard cases in
  `system-email-templates.spec.ts`; the tenant shadow-row retirement is a manual
  DB-backed step in the scenario, because `seed-config.ts` builds a Prisma client
  at import.

As filed, the record required:

An API unit test over `SYSTEM_EMAIL_TEMPLATE_PLACEHOLDERS` must assert that:
- no seed body or description contains "placeholder";
- every ACTIVE seed's template variables are a subset of its declared `availableVariables`.

The test fails today. REG entry to be added when the test exists.

## Dependencies

- Must ship together with [[BUG-3501]]. Removing the CONSOLE provider without real copy sends placeholders to real inboxes.
- ADR-0015.

## Related Items

- [[BUG-3501]]: sink providers and the provider screen.
- [[ITEM-0169]]: notification catalog hygiene (dead events).
- [[ITEM-0181]]: the visual template editor.
- [[ITEM-0180]]: the plain notification events page.
- [[BUG-3379]]: the NOT_DELIVERED delivery log reason.
- [[BUG-3506]]: follow-up — the payslip email passes a relative action link and
  the payroll calendar's name as the company, so its copy has no button or
  company name.
- [[ITEM-0194]]: follow-up — check production for tenant-owned copies of the old
  placeholder templates, which the seed only warns about. Checked read-only on
  2026-09-13: 0 found; closed DONE.

## Resolution

Fixed in TASK-0031 WP-04 (commit b0d8278d on `agent/walkthrough2-email-templates`,
merged into `agent/walkthrough2-integration`; plan EXECPLAN-0048), shipping with
[[BUG-3501]] per ADR-0015.

- **Authored copy.** `services/api/src/modules/notifications/system-email-templates.copy.ts`
  (new) holds subject, inline-styled HTML, plain text and labelled variables with
  sample values for all 11 templates. Each template's variables are the
  intersection of what its emitters pass. The owner review copy,
  `docs/tasks/TASK-0031-streams/WP-04-email-copy-for-owner-review.md`, is
  generated from it.
- **No fallback.** `notification-events.catalog.ts` builds the system templates
  only from authored copy and throws at module load when an event names a
  template with no copy.
- **Status rule.** ACTIVE only where something sends the event by email today:
  account activation, password reset, invoice, payslip, scheduled report, support
  case. DRAFT for `AUTH_OTP`, `LEAVE_APPROVAL_REQUEST`, `LEAVE_APPROVED`,
  `TIMESHEET_APPROVAL_REQUEST` and `PAYROLL_PROCESSED`.
- **Seed guard.** `planSystemTemplateWrite` creates a missing row, refreshes only
  an untouched system default (`tenantId null`, `isSystem`, `updatedBy null`) and
  skips everything else; `seedSystemEmailTemplates` and
  `NotificationsRepository.bootstrapSystemDefaults` both use it.
- **Seed verification.** `verifySystemEmailTemplates` fails the seed when an
  ACTIVE system row contains placeholder wording, and warns without changing
  tenant-owned rows. A read-only production check on 2026-09-13 found 0
  tenant-owned ACTIVE templates containing placeholder copy, so no tenant row
  needs an owner decision (ITEM-0194, closed DONE).
- **Shadow rows retired (found while fixing).** `seedTenantEmailTemplates` had
  written hidden ACTIVE `isSystem` auth templates into every tenant's scope; they
  outranked the system templates and blocked Customize. It now re-keys each such
  row, never saved by a person, to `<key>.retired-tenant-default` and archives
  it: idempotent, nothing deleted.
- **Latent send failure (found while fixing).** The system password reset
  template declared `primaryColor` and `logoUrl`, which no reset call site
  passes; retiring the shadow rows without the authored variables would have
  stopped forgot-password email.

Stream validation: api notifications and report-scheduler specs 20 suites / 184
tests passed; seed files typecheck; both mutation checks caught. The seed run
itself is a manual step on a throwaway database.

## QA Retest

**PASS.** Browser-verified in the local QA run
(`docs/qa/runs/2026-09-13-task-0031-demo-walkthrough-2-local-browser-qa-e253306.md`,
scenario S16) on a throwaway database. On production after release PR #80
(merge commit e253306a, deployed 2026-09-13): `/settings/notifications/templates`
lists account activation, password reset and scheduled report as ACTIVE system
defaults, with no placeholder copy. CI runs 34732185363, 34732682935 and
34732697734 passed on f865ac5e (the merged tree). Scenario QA-SETTINGS-027:

1. Run `npm --workspace api run seed:config` twice: the second run logs 0
   created, 11 refreshed, 0 left unchanged, with no verification error; SYSTEM
   rows match the owner review document; per-tenant `AUTH_*` `isSystem` rows are
   re-keyed `.retired-tenant-default` and ARCHIVED.
2. A forgot-password email arrives with the new copy.
3. Customize on Password reset creates a DRAFT tenant copy with no unique
   constraint error.
4. On the demo tenant after release, "Password reset email" shows the new copy
   and the scheduled report email arrives with the new copy.

## History

- 2026-09-13 — created from the second demo walkthrough (browser QA on the live demo tenant at df0f84f1); disposition set by the Architect after owner decisions.
- 2026-09-13 — fixed in TASK-0031 WP-04; unit-tested; browser verification pending. Follow-ups BUG-3506 and ITEM-0194 filed.
- 2026-09-13 — QA retest: PASS — local browser QA run and production verification at e253306a.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-backlog.mjs; edit the frontmatter, not this block -->

## Related

- Backlog item — [[ITEM-0194]]
- Referenced by — [[ITEM-0181]]
- Modules — [[notifications]], [[tenant-application]]
- Implementation — [[EXECPLAN-0048-email-template-default-copy-and-visual-editor]]
- Regression — REG-505 (see the regression register)

<!-- GRAPH:END -->
