---
SCENARIO_ID: QA-RUNTIME-033
aliases: [QA-RUNTIME-033]
TITLE: A lifecycle nobody could see
AREA: leave
MODULE: employees, organization, leave
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/audit/lifecycle-audit-coverage.spec.ts
RELATED_BUGS: [BUG-2044]
RELATED_REGRESSIONS: [REG-355]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-RUNTIME-033 — A lifecycle nobody could see

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-355 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

The coverage spec enumerates the write methods each service exposes **at runtime, off the prototype**, and requires every one to sit in either an `audited` list or an `exempt` map carrying its reason - so a method added later appears in neither and fails, and a renamed one fails from the other direction. The behavioural specs then assert the rows themselves: employee creation and reporting-manager assignment in both directions; department and designation create, update and delete; leave type, policy, policy rule and policy assignment creation; leave type and policy updates with both snapshots; leave request submission. Each module also asserts the negative - an operation the service refused writes no row - and that the row is written against the acting user's tenant, never one from the payload.

## Expected Result

The behaviour holds as REG-355 describes it. The automated guard is `services/api/src/modules/audit/lifecycle-audit-coverage.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

Creating an employee wrote no audit row. Neither did assigning a reporting manager, creating a department or designation, creating a leave policy, rule or assignment, changing a leave type, or submitting a leave request

## Notes

Generated from REG-355 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-355
- Bug records — BUG-2044

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-023]]
- Bugs — [[BUG-2044]]
- Regressions — REG-355 (see the regression register)

<!-- GRAPH:END -->
