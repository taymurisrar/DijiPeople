---
SCENARIO_ID: QA-RUNTIME-029
aliases: [QA-RUNTIME-029]
TITLE: A notification lifecycle with no resolution half
AREA: leave
MODULE: notifications, leave
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/notifications/notification-action-resolution.spec.ts
RELATED_BUGS: [BUG-2016]
RELATED_REGRESSIONS: [REG-331]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-RUNTIME-029 — A notification lifecycle with no resolution half

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-331 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

The mechanics: both tables written, tenant-scoped, `readAt` filled only where it was empty, `archivedAt` set, and nothing written at all when the record has no outstanding request for action. The call sites: cancel, approve and reject each resolve the notification keyed on that leave request, and on the decision path resolution runs **before** the employee's outcome notification is emitted.

## Expected Result

The behaviour holds as REG-331 describes it. The automated guard is `services/api/src/modules/notifications/notification-action-resolution.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

A notification's lifecycle ran only forwards from delivery, and every transition belonged to the recipient: `markInAppNotificationRead` and `archiveInAppNotification`, both keyed on a recipient row and both called from the user's own inbox actions. Nothing took a **record** and retired the outstanding requests for action pointing at it, so there was no call for a domain module to make

## Notes

Generated from REG-331 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-331
- Bug records — BUG-2016

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-023]]
- Bugs — [[BUG-2016]]
- Regressions — REG-331 (see the regression register)

<!-- GRAPH:END -->
