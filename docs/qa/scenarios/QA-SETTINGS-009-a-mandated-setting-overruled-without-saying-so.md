---
SCENARIO_ID: QA-SETTINGS-009
aliases: [QA-SETTINGS-009]
TITLE: A mandated setting overruled without saying so
AREA: settings
MODULE: tenant-settings
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/tenant-settings/attendance-settings-mandate.spec.ts
RELATED_BUGS: [BUG-1979]
RELATED_REGRESSIONS: [REG-320]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-SETTINGS-009 — A mandated setting overruled without saying so

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-320 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

A submitted value contradicting the mandate is refused with `ATTENDANCE_SETTING_ENFORCED_BY_PLATFORM`, naming the key, and nothing is written — including the other keys in the same submission. A value that matches the mandate is accepted as an ordinary no-op. `locationRequiredForModes` compares by set, so a different array order is not read as an attempted change. A contradicting value already stored is rewritten to the mandate on the next accepted write. Unmandated attendance keys and identically-named keys in other categories are untouched. All seven controls render disabled and each says why.

## Expected Result

The behaviour holds as REG-320 describes it. The automated guard is `services/api/src/modules/tenant-settings/attendance-settings-mandate.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`enforceCriticalAttendanceSetting` replaced the submitted value of seven attendance keys with the mandated constant on every write, inside `normalizeSettingUpdates` and therefore **before** the change-diff. Because the mandated value usually equalled what was already stored, the update was dropped as a no-op: the response echoed the mandated value, the audit row recorded no change, and nothing distinguished the refusal from "you saved the value you already had"

## Notes

Generated from REG-320 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-320
- Bug records — BUG-1979

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-021]]
- Module — [[settings]]
- Bugs — [[BUG-1979]]
- Regressions — REG-320 (see the regression register)

<!-- GRAPH:END -->
