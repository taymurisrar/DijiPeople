---
SCENARIO_ID: QA-BILLING-024
aliases: [QA-BILLING-024]
TITLE: A provider callback that arrived early, refused as a bad payload
AREA: billing
MODULE: billing, platform-events
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/billing/services/webhook-event-not-ready.spec.ts
RELATED_BUGS: [BUG-1543]
RELATED_REGRESSIONS: [REG-351]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-BILLING-024 — A provider callback that arrived early, refused as a bad payload

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-351 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

With an unactivated `SubscriptionOrder` for the event's Stripe customer, `processStripeEvent` answers `INTEGRATION_EVENT_NOT_READY` (409, not 400), names the order number, leaves the stored event `RECEIVED` so Stripe's redelivery reprocesses it, and records the platform event as `IGNORED` so no critical alert fires. With **no** order in flight the same event still fails, still records `FAILED` and still alerts. An unknown Stripe event type is ignored and answered 200. The controller declares no `@Body()`, still takes the raw request and the signature header, and still calls `verifyWebhookSignature`.

## Expected Result

The behaviour holds as REG-351 describes it. The automated guard is `services/api/src/modules/billing/services/webhook-event-not-ready.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

A public self-service signup has no tenant until the payment authorises provisioning to create one, and Stripe's `customer.subscription.created` and `invoice.paid` callbacks routinely arrive before provisioning finishes. `resolveSubscriptionContext` and `resolveInvoiceContext` then found nothing to attribute the event to and threw `BadRequestException`, which `HttpExceptionFilter` renders as `400 VALIDATION_FAILED` — the status asserting the *caller* sent something malformed

## Notes

Generated from REG-351 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-351
- Bug records — BUG-1543

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-020]]
- Module — [[billing]]
- Bugs — [[BUG-1543]]
- Regressions — REG-351 (see the regression register)

<!-- GRAPH:END -->
