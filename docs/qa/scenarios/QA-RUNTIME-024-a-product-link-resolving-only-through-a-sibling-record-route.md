---
SCENARIO_ID: QA-RUNTIME-024
aliases: [QA-RUNTIME-024]
TITLE: A product link resolving only through a sibling record route
AREA: runtime-modules
MODULE: apps/web runtime
TYPE: E2E
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: apps/web/lib/routing/route-integrity.spec.ts
RELATED_BUGS: [BUG-2004, BUG-2014]
RELATED_REGRESSIONS: [REG-314]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-RUNTIME-024 — A product link resolving only through a sibling record route

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-314 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Two blocks. For every `StandardModuleRuntimeSpec` in both spec files, either `adapterCapabilities.disableCreate` is declared or `<routeBase>/new` resolves to a real page **without** a dynamic folder consuming its last segment — 21 modules. And every literal internal link in `apps/web/app` — `href`-ish properties, `router.push`/`redirect` calls, and named route constants — resolves to a page under the same condition: 50 links. Both blocks assert they found something to check, because an `it.each` over an empty array is green.

## Expected Result

The behaviour holds as REG-314 describes it. The automated guard is `apps/web/lib/routing/route-integrity.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

Three links pointed at paths with no page, so Next matched each against the sibling dynamic detail route with the literal path segment as the record id. `/approvals/new` was **generated**, not hand-written: `approvalRuntimeSpec` omitted `adapterCapabilities.disableCreate`, so the standard runtime emitted a `system.new` command and promoted it to a primary button; it resolved to `approvals/[approvalId]` with `approvalId === "new"`, fetched an approval that cannot exist, and threw uncaught into the error boundary

## Notes

Generated from REG-314 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-314
- Bug records — BUG-2004, BUG-2014

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-011]]
- Bugs — [[BUG-2004]], [[BUG-2014]]
- Regressions — REG-314 (see the regression register)

<!-- GRAPH:END -->
