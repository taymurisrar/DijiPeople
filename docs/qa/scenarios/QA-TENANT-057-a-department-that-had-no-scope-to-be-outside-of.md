---
SCENARIO_ID: QA-TENANT-057
aliases: [QA-TENANT-057]
TITLE: A department that had no scope to be outside of
AREA: tenant-isolation
MODULE: organization
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/organization/organization-read-scope.spec.ts
RELATED_BUGS: [BUG-1957]
RELATED_REGRESSIONS: [REG-310]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-TENANT-057 — A department that had no scope to be outside of

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-310 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

A department with a null business unit is visible to a tenant-scoped reader and fetchable by id; invisible to a parent-child reader and unfetchable by an organization-scoped one; and reachable by a tenant-scoped **manager**, which is the privilege that makes it repairable rather than merely visible. The pre-existing assertion that departments outside the caller's business units stay hidden is kept untouched as the control.

## Expected Result

The behaviour holds as REG-310 describes it. The automated guard is `services/api/src/modules/organization/organization-read-scope.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`OrganizationService.findDepartmentsForUser` filtered on `department.businessUnitId !== null && visibleBusinessUnitIds.has(department.businessUnitId)`, which answers false for two different situations: a department whose business unit the caller cannot see, and a department that has no business unit at all. Only the first is a scope decision; the second is a row with no scope to be outside of

## Notes

Generated from REG-310 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-310
- Bug records — BUG-1957

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-003]]
- Module — [[tenant-isolation]]
- Bugs — [[BUG-1957]]
- Regressions — REG-310 (see the regression register)

<!-- GRAPH:END -->
