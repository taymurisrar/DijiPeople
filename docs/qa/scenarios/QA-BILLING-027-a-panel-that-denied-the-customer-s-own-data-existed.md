---
SCENARIO_ID: QA-BILLING-027
aliases: [QA-BILLING-027]
TITLE: A panel that denied the customer's own data existed
AREA: billing
MODULE: tenant-control-plane, apps/admin
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/tenant-control-plane/tenant-localization-panel.spec.ts
RELATED_BUGS: [BUG-1977]
RELATED_REGRESSIONS: [REG-327]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-BILLING-027 — A panel that denied the customer's own data existed

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-327 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

The panel returns the tenant's country, timezone, locale, currency and date format rather than an empty object; `locale` is read from the `system` category; `dateFormat` is read from the `organization` category and not the system copy; the `TenantSetting` query never filters on a dotted composite, asserted against the serialised Prisma arguments; and `configured` is false, with the values still returned, when the tenant has written no row.

## Expected Result

The behaviour holds as REG-327 describes it. The automated guard is `services/api/src/modules/tenant-control-plane/tenant-localization-panel.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

The tenant configuration query filtered `TenantSetting.key` on `['organization.country', 'organization.timezone', ...]`. `category` and `key` are separate columns with `@@unique([tenantId, category, key])`, and no writer ever puts a dotted composite in `key` — both writers set the columns separately

## Notes

Generated from REG-327 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-327
- Bug records — BUG-1977

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-020]]
- Module — [[billing]]
- Bugs — [[BUG-1977]]
- Regressions — REG-327 (see the regression register)

<!-- GRAPH:END -->
