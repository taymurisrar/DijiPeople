---
SCENARIO_ID: QA-TENANT-061
aliases: [QA-TENANT-061]
TITLE: Two naming conventions and five columns nothing filled
AREA: tenant-isolation
MODULE: audit
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/audit/audit-actions.spec.ts
RELATED_BUGS: [BUG-2046]
RELATED_REGRESSIONS: [REG-356]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-TENANT-061 — Two naming conventions and five columns nothing filled

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-356 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Every catalog entry is `SCREAMING_SNAKE` and its key matches its value; the legacy alias map resolves each observed dotted name to exactly one canonical action; canonicalising a name already canonical is a no-op and an unknown name passes through unchanged rather than being dropped; a filter on a canonical action expands to every stored spelling, so one filter finds rows written under both conventions; a filter on a legacy name still finds them, proven against `AuditRepository.findByTenant` itself (the `where.action` it builds), not just the resolver function in isolation. The filter metadata the screen renders is deduplicated by canonical name. `audit-canonical-writes.spec.ts` separately reads `attendance.service.ts`, `projects.service.ts` and `auth.service.ts` and asserts each now writes through `AUDIT_ACTIONS.*` rather than the dotted literal this bug was filed against.

## Expected Result

The behaviour holds as REG-356 describes it. The automated guard is `services/api/src/modules/audit/audit-actions.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

Audit action names were free-form string literals at every call site across 82 files, so two conventions coexisted by accident - `SCREAMING_SNAKE` in most modules and `dot.lower_snake` in attendance, projects and auth, with the dotted set not even internally consistent about tense. Anyone filtering, alerting or exporting on `action` had to know which convention each module happened to use

## Notes

Generated from REG-356 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-356
- Bug records — BUG-2046

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-003]]
- Module — [[tenant-isolation]]
- Bugs — [[BUG-2046]]
- Regressions — REG-356 (see the regression register)

<!-- GRAPH:END -->
