---
SCENARIO_ID: QA-ATT-008
aliases: [QA-ATT-008]
TITLE: Every employee counted absent on a non-working day
AREA: attendance
MODULE: dashboard, attendance
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/attendance/attendance-work-day-resolution.spec.ts
RELATED_BUGS: [BUG-2008]
RELATED_REGRESSIONS: [REG-318]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-ATT-008 — Every employee counted absent on a non-working day

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-318 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

On a date the work schedule marks as an off day, the absent count is zero and no absence exception is raised; the same holds for a configured holiday on the employee's resolved calendar. A working day is unaffected — a genuine absence is still counted and still raised as a warning. An excused employee is not counted absent alongside working colleagues. The date handed to the attendance module is the UTC midnight of the day the tile is reporting on. The bulk resolver reproduces the single-employee precedence: assignment beats employee default beats team beats department; business-unit scope beats organization scope; tenant default is last; an employee with no schedule at all is **not** excused.

## Expected Result

The behaviour holds as REG-318 describes it. The automated guard is `services/api/src/modules/attendance/attendance-work-day-resolution.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`getAttendanceOperations` derived absence as `Math.max(activeEmployeeCount - entries.length, 0)` — headcount minus whoever had an attendance row — and consulted no work schedule and no holiday calendar. The same number fed the "Absent employees" exception row and the manager view's "Absent today" metric

## Notes

Generated from REG-318 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-318
- Bug records — BUG-2008

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-009]]
- Module — [[attendance]]
- Bugs — [[BUG-2008]]
- Regressions — REG-318 (see the regression register)

<!-- GRAPH:END -->
