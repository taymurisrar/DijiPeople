---
SCENARIO_ID: QA-TENANT-059
aliases: [QA-TENANT-059]
TITLE: Two list endpoints in one product, answering in two shapes
AREA: tenant-isolation
MODULE: organization
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/organization/departments-list-contract.spec.ts
RELATED_BUGS: [BUG-1959]
RELATED_REGRESSIONS: [REG-312]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-TENANT-059 — Two list endpoints in one product, answering in two shapes

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-312 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

With neither `page` nor `pageSize`, the response is the bare array, unchanged. With either, it is the employees envelope with server-computed `total` and `totalPages`. A page beyond the end clamps to the last page rather than returning nothing, and the page size defaults to the employees default when only a page is asked for.

## Expected Result

The behaviour holds as REG-312 describes it. The automated guard is `services/api/src/modules/organization/departments-list-contract.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`GET /api/departments` took `ListMasterDataDto`, which declares no pagination fields, so under the global `ValidationPipe`'s `forbidNonWhitelisted: true` a `pageSize` query parameter was a 400 — "property pageSize should not exist" — while `GET /api/employees` answered with the `{items, meta}` envelope. Every department row was shipped on every load, and any consumer written against the employees envelope broke when pointed at departments.

## Notes

Generated from REG-312 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-312
- Bug records — BUG-1959

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-003]]
- Module — [[tenant-isolation]]
- Bugs — [[BUG-1959]]
- Regressions — REG-312 (see the regression register)

<!-- GRAPH:END -->
