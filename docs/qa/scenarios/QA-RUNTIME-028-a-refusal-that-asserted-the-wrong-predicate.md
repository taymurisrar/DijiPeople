---
SCENARIO_ID: QA-RUNTIME-028
aliases: [QA-RUNTIME-028]
TITLE: A refusal that asserted the wrong predicate
AREA: approvals
MODULE: approvals
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/approvals/approval-matrices.service.spec.ts
RELATED_BUGS: [BUG-1969]
RELATED_REGRESSIONS: [REG-330]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-RUNTIME-028 — A refusal that asserted the wrong predicate

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-330 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

An invited approver refused with a message about account activation; a disabled one refused in its own words; a user genuinely outside the tenant still getting the exact original tenancy sentence; an active one created successfully.

## Expected Result

The behaviour holds as REG-330 describes it. The automated guard is `services/api/src/modules/approvals/approval-matrices.service.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`ApprovalMatricesService.validate` tested a candidate approver with `ApprovalMatrixRepository.findUserById`, whose `where` is `{ tenantId, id, status: 'ACTIVE' }` — one query answering two questions — and reported only the tenant half: "Selected approver user does not belong to this tenant." An `INVITED` user the tenant had just provisioned, and whom `GET /api/users` returns to the same caller, was therefore told a false fact about the caller's own data, sending an administrator to look for a cross-tenant mistake that never happened.

## Notes

Generated from REG-330 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-330
- Bug records — BUG-1969

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-022]]
- Module — [[approvals]]
- Bugs — [[BUG-1969]]
- Regressions — REG-330 (see the regression register)

<!-- GRAPH:END -->
