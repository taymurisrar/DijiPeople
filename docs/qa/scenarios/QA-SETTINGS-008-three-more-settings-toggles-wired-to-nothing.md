---
SCENARIO_ID: QA-SETTINGS-008
aliases: [QA-SETTINGS-008]
TITLE: Three more settings toggles wired to nothing
AREA: settings
MODULE: timesheets
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/timesheets/timesheet-audit-settings.service.spec.ts
RELATED_BUGS: [BUG-2206]
RELATED_REGRESSIONS: [REG-328]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-SETTINGS-008 — Three more settings toggles wired to nothing

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-328 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

On, off and unset for each of the three, as the record's acceptance criteria require. Plus the fail-open path when settings cannot be read, a stored string `"false"` treated as off because `TenantSetting.value` is `Json`, and a tenant-isolation assertion that the category read is scoped to the calling tenant.

## Expected Result

The behaviour holds as REG-328 describes it. The automated guard is `services/api/src/modules/timesheets/timesheet-audit-settings.service.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

The `timesheets` category declares four audit toggles and all four rendered as live checkboxes. REG-308 wired `auditBackgroundJobs`; `auditEntryChanges`, `auditPolicyResolution` and `auditExports` appeared only in the catalog and in the settings page config, with no consumer anywhere in `services/api/src`

## Notes

Generated from REG-328 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-328
- Bug records — BUG-2206

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-021]]
- Module — [[settings]]
- Bugs — [[BUG-2206]]
- Regressions — REG-328 (see the regression register)

<!-- GRAPH:END -->
