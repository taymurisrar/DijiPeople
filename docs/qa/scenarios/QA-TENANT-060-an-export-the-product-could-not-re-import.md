---
SCENARIO_ID: QA-TENANT-060
aliases: [QA-TENANT-060]
TITLE: An export the product could not re-import
AREA: tenant-isolation
MODULE: employees
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/employees/employees.export-import-contract.spec.ts
RELATED_BUGS: [BUG-2026]
RELATED_REGRESSIONS: [REG-357]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-TENANT-060 — An export the product could not re-import

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-357 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Seven tests. The export header row equals the template header row; the template equals the declared `EMPLOYEE_IMPORT_COLUMNS` contract; every contract column is **populated** for a fully-populated employee; the manager code, split name and emergency-contact values are the exported values; a three-column view selection still yields the full contract; an empty export still carries its header row; and a **round trip** feeds the bytes `exportEmployees()` produced straight back into `importEmployees()` unedited, which parses the row and calls `create` with department, designation and relation type resolved back into ids.

## Expected Result

The behaviour holds as REG-357 describes it. The automated guard is `services/api/src/modules/employees/employees.export-import-contract.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`GET /employees/export` and `GET /employees/export-template` were built independently and each owned its own column list. The export emitted eleven human titles from `EMPLOYEE_EXPORT_COLUMN_LABELS` (`Employee Code`, `Full Name`, `Reporting Manager`, …); the template emitted twenty-one field keys from a second list inlined in `exportEmployeeTemplate()`

## Notes

Generated from REG-357 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-357
- Bug records — BUG-2026

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-003]]
- Module — [[tenant-isolation]]
- Bugs — [[BUG-2026]]
- Regressions — REG-357 (see the regression register)

<!-- GRAPH:END -->
