---
SCENARIO_ID: QA-ATT-012
aliases: [QA-ATT-012]
TITLE: The create-branch fallback skipped the two grace fields and the office-location switch
AREA: attendance
MODULE: attendance
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/attendance/attendance-policy-write.spec.ts
RELATED_BUGS: [BUG-1980]
RELATED_REGRESSIONS: [REG-359]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-ATT-012 — The create-branch fallback skipped the two grace fields and the office-location switch

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-359 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Creating the policy row with the three fields omitted from the DTO seeds them from the effective settings values (`defaultGraceMinutes`, `enforceOfficeLocationForOfficeMode`), not from the column default, asserted both directly on the create-branch payload and round-tripped through `getPolicy`/`resolvePolicy` the way a client would actually observe it â€” the exact repro from the bug record's Reproduction section. An unrelated field is flipped in the same call to prove the omission itself, not a same-value patch, is what is under test.

## Expected Result

The behaviour holds as REG-359 describes it. The automated guard is `services/api/src/modules/attendance/attendance-policy-write.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

The 2026-08-29 fix for this same bug (REG-323) made `updatePolicy`'s create branch fall back to the currently effective policy value for nine fields the caller's DTO might omit, closing the "creating the row silently resets Settings" defect for those nine. Three more fields consulted the identical way by `resolvePolicy` â€” `lateCheckInGraceMinutes`, `lateCheckOutGraceMinutes` (both fed by `attendanceSettings.defaultGraceMinutes`) and `requireOfficeLocationForOfficeMode` (fed by `attendanceSettings.enforceOfficeLocationForOfficeMode`) â€” were missed: `attendance.service.ts` wrote `dto.lateChe

## Notes

Generated from REG-359 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-359
- Bug records — BUG-1980

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-009]]
- Module — [[attendance]]
- Bugs — [[BUG-1980]]
- Regressions — REG-359 (see the regression register)

<!-- GRAPH:END -->
