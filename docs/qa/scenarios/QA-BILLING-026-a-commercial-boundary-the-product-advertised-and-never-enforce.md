---
SCENARIO_ID: QA-BILLING-026
aliases: [QA-BILLING-026]
TITLE: A commercial boundary the product advertised and never enforced
AREA: billing
MODULE: tenant-settings, apps/web, and every module a plan can withhold
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/common/guards/entitlement.guard.spec.ts
RELATED_BUGS: [BUG-1952]
RELATED_REGRESSIONS: [REG-353]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-BILLING-026 — A commercial boundary the product advertised and never enforced

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-353 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

An entitled module is allowed and an unentitled one refused with `TENANT_FEATURE_NOT_ENTITLED`, a code distinct from `ACCESS_DENIED`. **Every** elevated tenant role is still refused, driven off the real `ELEVATED_TENANT_ROLE_KEYS` set rather than named roles. A platform user is exempt without resolving anything. A cold-cache resolver failure answers 503 `TENANT_ENTITLEMENT_UNAVAILABLE`; a warm-cache failure serves the last snapshot. Report-only logs and allows; `OFF` resolves nothing. Structurally: every controller in a gated module carries the guard and its module's key, no controller outside the decided set carries the decorator, and every feature key the plan catalog can withhold is either gated or recorded as exempt with a reason.

## Expected Result

The behaviour holds as REG-353 describes it. The automated guard is `services/api/src/common/guards/entitlement.guard.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

Plan entitlements were resolved, stored, rendered on the platform-admin module screen, served to the tenant web shell at `/tenant-settings/features/availability` and sold on the public plans page — and consulted by nothing on any request path. `FeatureAccessService.assertFeatureEnabled` (`feature-access.service.ts:78-86`) was the only throwing entitlement primitive in the API and had **zero call sites**: one occurrence in the repository, its own definition

## Notes

Generated from REG-353 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-353
- Bug records — BUG-1952

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-020]]
- Module — [[billing]]
- Bugs — [[BUG-1952]]
- Regressions — REG-353 (see the regression register)

<!-- GRAPH:END -->
