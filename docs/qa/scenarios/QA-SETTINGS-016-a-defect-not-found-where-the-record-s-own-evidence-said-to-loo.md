---
SCENARIO_ID: QA-SETTINGS-016
aliases: [QA-SETTINGS-016]
TITLE: A defect not found where the record's own evidence said to look
AREA: settings
MODULE: apps/web settings runtime
TYPE: MANUAL_VISUAL
RISK: MEDIUM
AUTOMATION_STATUS: MANUAL
TEST_REFERENCE: 
RELATED_BUGS: [BUG-1960]
RELATED_REGRESSIONS: [REG-345]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-SETTINGS-016 — A defect not found where the record's own evidence said to look

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-345 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Source-level (no jsdom, no browser available to this task): the settings layout's content column uses `minmax(0,1fr)`; the module page layout's children wrapper carries `min-w-0`; the shared data table's root carries `w-full min-w-0` and its table wrapper carries `overflow-x-auto`.

## Expected Result

The behaviour holds as REG-345 describes it. There is no automated guard, so this scenario is executed by hand and its result recorded here.

## Why This Is Reusable

None — investigated and does not reproduce. The record measured a departments table overflowing its settings panel by 111px on the production demo tenant (deployed commit `949f461c`), but every layer of the render chain between the panel and the table already carries the containment its own Proposed Resolution asked for: `settings-layout.tsx`'s content column is `grid-cols-[minmax(0,1fr)]` (not a bare `grid`, which would size to its widest child), `module-page-layout.tsx`'s `{children}` wrapper carries `min-w-0` (closing the separate "a grid/flex item's own default min-width is auto" trap that

## Notes

Generated from REG-345 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-345
- Bug records — BUG-1960

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-021]]
- Module — [[settings]]
- Bugs — [[BUG-1960]]
- Regressions — REG-345 (see the regression register)

<!-- GRAPH:END -->
