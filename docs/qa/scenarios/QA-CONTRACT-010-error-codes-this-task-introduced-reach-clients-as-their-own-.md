---
SCENARIO_ID: QA-CONTRACT-010
aliases: [QA-CONTRACT-010]
TITLE: Error codes this task introduced reach clients as their own code, not a generic fallback
AREA: legal
MODULE: services/api/src/common/errors
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/common/errors/task-0032-error-codes.spec.ts
RELATED_BUGS: [BUG-3585]
RELATED_REGRESSIONS: [REG-624]
LAST_RUN: 
LAST_RESULT: NOT_RUN
CREATED_AT: 2026-09-25
UPDATED_AT: 2026-09-25
---

# QA-CONTRACT-010 — Error codes this task introduced reach clients as their own code, not a generic fallback

## Preconditions

None — a static check that every error code thrown anywhere in TASK-0032's
changed files is registered in `error-catalog.ts`.

## Steps

1. Trigger `PATCH /contracts/:id/document-fields` with a `signature.*` field
   (should surface `CONTRACT_SIGNATURE_FIELD_NOT_EDITABLE`).
2. Trigger an MFA lockout / a platform-authorization refusal introduced by
   WP-02/WP-03.
3. Run `task-0032-error-codes.spec.ts` directly.

## Expected Result

Steps 1-2 return the specific `errorCode` named by the throwing code, never
`SYSTEM_UNEXPECTED_ERROR`. Step 3 passes, and fails if a future change
reintroduces an uncatalogued code among the ones this task added.

## Notes

Created 2026-09-25 at `0d33172f`. Regression test:
`services/api/src/common/errors/task-0032-error-codes.spec.ts`.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-015]]
- Module — [[legal]]
- Bugs — [[BUG-3585]]
- Regressions — REG-624 (see the regression register)

<!-- GRAPH:END -->
