---
SCENARIO_ID: QA-ATT-011
aliases: [QA-ATT-011]
TITLE: Saving one screen froze the settings behind another
AREA: attendance
MODULE: attendance
TYPE: UNIT
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: services/api/src/modules/attendance/attendance-policy-write.spec.ts
RELATED_BUGS: [BUG-1980]
RELATED_REGRESSIONS: [REG-323]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-ATT-011 — Saving one screen froze the settings behind another

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-323 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Creating the policy row seeds every omitted optional field from the currently *effective* value rather than from a constant — asserted on six fields whose effective values all differ from the constants that used to be written, so a regression cannot pass by coincidence. An explicitly submitted value still wins over the effective one. Both halves of the upsert are scoped to the caller's tenant.

## Expected Result

The behaviour holds as REG-323 describes it. The automated guard is `services/api/src/modules/attendance/attendance-policy-write.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`resolvePolicy` reads each value as `policy?.X ?? attendanceSettings.X`, and every `AttendancePolicy` column consulted that way is non-nullable with a Prisma default — so the fallback fires only when the whole **row** is absent, not per field. The row is not seeded; it appears the first time anyone saves the attendance policy screen

## Notes

Generated from REG-323 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-323
- Bug records — BUG-1980

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-009]]
- Module — [[attendance]]
- Bugs — [[BUG-1980]]
- Regressions — REG-323 (see the regression register)

<!-- GRAPH:END -->
