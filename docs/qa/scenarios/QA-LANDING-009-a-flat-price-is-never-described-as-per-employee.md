---
SCENARIO_ID: QA-LANDING-009
aliases: [QA-LANDING-009]
TITLE: A flat price is never described as per-employee
AREA: landing
MODULE: apps/landing/lib/plan-presentation.ts
TYPE: UNIT
RISK: HIGH
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: apps/landing/lib/plan-presentation.spec.ts
RELATED_BUGS: [BUG-0080]
RELATED_REGRESSIONS: [REG-075]
LAST_RUN: 2026-08-20
LAST_RESULT: PASS
CREATED_AT: 2026-08-20
UPDATED_AT: 2026-08-20
---

# QA-LANDING-009 — A flat price is never described as per-employee

## Preconditions

None. `billingUnitLabel` is a pure function over a resolved commercial offer.

## Steps

1. Render a `FLAT` monthly offer and read the unit label.
2. Render a `FLAT` annual offer.
3. Render a `PER_SEAT` monthly offer.
4. Render an unavailable offer, and `null`.

## Expected Result

1. `per month` — and no mention of employees, seats or users.
2. `per year`.
3. `per active employee / month`. The product sells both models, and the two
   answers must not collapse into one.

   **The direction reversed on 2026-08-20** and this line used to read "public
   plans are flat, and a negotiated per-seat price stays available". It is now
   the other way round: public and self-service plans are **per active
   employee**, and flat is the negotiated one, reachable only through an
   operator. The assertion is unchanged, because what it guards is that a label
   matches its `billingModel` — not which model the public happens to be sold.
4. `null` in both cases. A price nobody can buy gets no unit.

## Notes

The `copy-contradicts-configuration` pattern, and the reason it survived so long
is instructive: **the arithmetic was never wrong.** `estimateCost` and
`calculateSeatPricing` both refuse to multiply a flat price, each with a comment
explaining why. Every number the customer saw was correct. Only the words around
them claimed a model the configuration did not use — the Terms, the billing
terms, the features page, the plans metadata, the plans hero and the estimator
all said "per active employee".

`billingUnitLabel` returning `null` for a flat price is what tipped it from
sloppy to misleading: the figure rendered with no unit at all, directly beneath
a heading saying the price was per person, leaving the reader to do a
multiplication nobody would ever charge them for.

Worth keeping in mind when judging severity here: the error overstated the
price. A 500-person company read `$199 per active employee` and concluded
DijiPeople cost $99,500 a month. The cost of this defect was in sales that never
happened, which is the kind that leaves no trace in any log.

**Re-reading that in 2026-08-20's world is instructive.** The public model is
now genuinely per active employee, so the label the old copy used is the correct
one today — at a real rate of PKR 300 to 900, not a flat total mislabelled as a
unit price. The defect was never the words on their own; it was the words
disagreeing with `billingModel`. That is why this scenario asserts the pairing
rather than any particular wording, and why it survived the model changing
underneath it.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-013]]
- Bugs — [[BUG-0080]]
- Regressions — REG-075 (see the regression register)

<!-- GRAPH:END -->
