---
SCENARIO_ID: QA-SETTINGS-014
aliases: [QA-SETTINGS-014]
TITLE: A singular derived by deleting a letter, and a title never derived at all
AREA: settings
MODULE: apps/web settings runtime, apps/web runtime related lists
TYPE: E2E
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: apps/web/lib/text/inflection.spec.ts
RELATED_BUGS: [BUG-1964]
RELATED_REGRESSIONS: [REG-339]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-SETTINGS-014 — A singular derived by deleting a letter, and a title never derived at all

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-339 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

`singularize()` handles irregular plurals, `-ies`/`-es` endings and words already singular that a trailing-`s` strip would mangle ("Status", "Address"), and returns an unrecognised word unchanged. Over the source: the settings registry no longer contains the `replace(/s$/, "")` literal and does contain `singularize(input.label)`, with a declared `input.singular` still taking priority; the related-subgrid dialog titles read `New ${singularize(...)}` at both call sites, not the raw label.

## Expected Result

The behaviour holds as REG-339 describes it. The automated guard is `apps/web/lib/text/inflection.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

The settings adapter registry derived every record header's singular from its plural with `input.label.replace(/s$/, "")`, so "Leave Policies" produced "Leave Policie". The related-list quick-create dialog titles (`buildGenericQuickCreate`, `buildSubgridQuickCreate` in `module-related-subgrid.tsx`) applied no derivation at all, printing the tab's plural title verbatim — "New Entitlements", "New Assignments"

## Notes

Generated from REG-339 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-339
- Bug records — BUG-1964

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-021]]
- Module — [[settings]]
- Bugs — [[BUG-1964]]
- Regressions — REG-339 (see the regression register)

<!-- GRAPH:END -->
