---
SCENARIO_ID: QA-RUNTIME-036
aliases: [QA-RUNTIME-036]
TITLE: ARIA that described a listbox nobody had built
AREA: runtime-modules
MODULE: apps/web metadata form runtime, shared form controls
TYPE: E2E
RISK: MEDIUM
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: apps/web/lib/a11y/listbox-navigation.spec.ts
RELATED_BUGS: [BUG-1956]
RELATED_REGRESSIONS: [REG-338]
LAST_RUN: 2026-08-30
LAST_RESULT: PASS
CREATED_AT: 2026-08-30
UPDATED_AT: 2026-08-30
---

# QA-RUNTIME-036 — ARIA that described a listbox nobody had built

## Preconditions

A tenant on which the affected surface is reachable, and a signed-in user with
the permissions the surface requires. REG-338 in the regression register carries
the full root cause; this scenario is the reusable check, not the diagnosis.

## Steps

Movement is a pure function: arrows wrap, Home and End jump, a non-movement key yields nothing, an empty list yields no index, and `aria-activedescendant` resolves to `undefined` rather than to an id naming no element. Over the source: as many listboxes as comboboxes, an option role and a selected state for each, no `type="button"` inside a listbox, no link inside a combobox trigger, and no unconditional `aria-controls`.

## Expected Result

The behaviour holds as REG-338 describes it. The automated guard is `apps/web/lib/a11y/listbox-navigation.spec.ts`; a run of that suite is sufficient evidence for this scenario.

## Why This Is Reusable

`form-control.tsx` has two composite controls, `LookupField` and `SelectField`, and both announced `role="combobox"`, `aria-haspopup="listbox"` and `aria-controls` over a popup that was a plain `div` of `button` elements. The buttons are the root of it: they are why there was no `role="option"`, no `aria-selected` and no `aria-activedescendant` — the popup was navigated with Tab, so there was no "active" option to name — and they are also a `nested-interactive` violation, every option being a focusable child of a widget role

## Notes

Generated from REG-338 during the SESSION-0076 open-bug sweep, so the scenario and
the register entry cannot disagree about what is guarded — a scenario written
separately would be a second copy of the same facts, free to drift. Anything the
register entry does not state is left blank here rather than invented.

## Related

- Regression — REG-338
- Bug records — BUG-1956

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-011]]
- Bugs — [[BUG-1956]]
- Regressions — REG-338 (see the regression register)

<!-- GRAPH:END -->
