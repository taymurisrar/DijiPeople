---
SCENARIO_ID: QA-DEPLOY-021
aliases: [QA-DEPLOY-021]
TITLE: The production dependency graph carries no critical advisory
AREA: deployment-release
MODULE: package-lock.json
TYPE: SECURITY
RISK: HIGH
AUTOMATION_STATUS: AUTOMATED
TEST_REFERENCE: scripts/check-production-advisories.mjs
RELATED_BUGS: [BUG-0052]
RELATED_REGRESSIONS: [REG-217]
LAST_RUN: 
LAST_RESULT: NOT_RUN
CREATED_AT: 2026-08-22
UPDATED_AT: 2026-08-22
---

# QA-DEPLOY-021 — The production dependency graph carries no critical advisory

## Preconditions

A checkout of the repository with `package-lock.json` as committed.

## Steps

1. `npm run check:production-advisories`.
2. `npm run check:overrides-applied`.
3. Deeper form: `npm ci` into a scratch copy of the manifests, then
   `npm audit --omit=dev` against the **installed tree** rather than the
   lockfile.

## Expected Result

Zero critical advisories. Every surviving advisory has a written disposition,
and a disposition matching no advisory fails — a risk acceptance for a package
that is no longer vulnerable reads as a live one.

The installed tree carries `@mapbox/node-pre-gyp` 2.x exporting `find`,
`node-gyp` 11.x, `tar` 7.x nested under both, and no hoisted
`tar`/`cacache`/`make-fetch-happen`.

Step 3 matters: two dispositions have already been wrong because a
reachability claim was made by inspection rather than by looking at the
artifact.

## Notes

Created 2026-08-22 at `9d4f115`.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-qa.mjs; edit the frontmatter, not this block -->

## Related

- Test plan — [[PLAN-012]]
- Bugs — [[BUG-0052]]
- Regressions — REG-217 (see the regression register)

<!-- GRAPH:END -->
