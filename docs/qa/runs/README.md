# QA Runs

One file per QA execution: `YYYY-MM-DD-<feature-slug>-<short-sha>.md`.

These are **history** — never edit a run after the fact. Scaffold one with:

```bash
node scripts/new-qa-run.mjs <feature-slug>
```

Empty until the first run.
