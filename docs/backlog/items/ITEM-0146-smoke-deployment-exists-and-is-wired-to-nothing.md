---
ID: ITEM-0146
aliases: [ITEM-0146]
Title: smoke:deployment exists and is wired to nothing
Type: INFRA
Status: DEFERRED
Priority: P2
Severity: MEDIUM
AffectedModules: [.github/workflows]
Source: REVIEWER
OwnerAgent: architect
ArchitectDisposition: DEFER
CreatedAt: 2026-09-10
UpdatedAt: 2026-09-11
RelatedBug: 
RelatedQA: 
RelatedADR: 
RelatedImplementation:
TargetMilestone: 
BlockedBy: 
---

# ITEM-0146 — smoke:deployment exists and is wired to nothing

> **Architect triage, 2026-09-11 — `DEFER`.** Real and recorded, below the line for this cycle. Revisit at the next backlog review — the evidence is in the record, so nothing is lost by scheduling it later.

## Summary

smoke:deployment exists and is wired to nothing

Identified by the 2026-09-10 full technical audit as CI-12 (confidence: CI-12=CONFIRMED).

## Why It Matters

the window between "deploy went live" and "someone noticed it is
  broken" is unbounded and human-paced. There is no automatic rollback to shorten
  it (CI-09).

## Evidence

**CI-12** (`scripts/smoke-deployment.mjs`, `.github/workflows/`):

```
  $ grep -rn "smoke" .github/workflows/
  (no matches)
  ```
  The script is a root npm script (`smoke:deployment = node scripts/smoke-deployment.mjs`)
  and is documented as a manual step:
  `docs/deployment/deployment-runbook.md:80-82` — *"8. Smoke tests …
  `npm run smoke:deployment` with `SMOKE_API_BASE_URL` and credentials set."*

  The runbook is candid about why this matters:
  ```
  docs/deployment/deployment-runbook.md:61-63
  ⚠️ `status` is hardcoded `ok` and tests no dependency. **A 200 here does not
  mean the system is healthy** — proceed to smoke tests before believing it.
  ```
  And `.agent/context/deployment-runtime.md:167-170`: *"**CI validates a commit;
  nothing validates a deployment.**"*

---


Full finding text: CI-12 in `docs/engineering/audits/2026-09-10-full-technical-audit/raw/CI.md`.

## Proposed Approach

a `post-deploy` workflow triggered by a Render deploy webhook
  (or on a schedule) running `npm run smoke:deployment` against
  `https://dijipeople.onrender.com/api` with a smoke account, plus a check that
  `/api`'s `commit` field equals the expected SHA — a comparison that is now
  possible and was not before (see Healthy).

(Difficulty: MEDIUM; Regression risk: LOW; Fix now: LATER)

## Acceptance Criteria

- The behaviour described in Expected Behavior holds for `scripts/smoke-deployment.mjs`, `.github/workflows/` (audit id CI-12).

## Dependencies

None identified beyond the work itself.

## Related Items

- Audit finding `CI-12` — `docs/engineering/audits/2026-09-10-full-technical-audit/raw/CI.md`

## History

- 2026-09-10 — created at `4c86a29b`.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-backlog.mjs; edit the frontmatter, not this block -->

## Related

- No related record, module or decision is declared in this record's
  frontmatter. Declare one rather than adding a link here by hand — this
  block is regenerated and a hand-written link inside it is lost.

<!-- GRAPH:END -->
