---
TASK_ID: TASK-0021
aliases: [TASK-0021]
TITLE: Release the tenant list identity fix and the first admin browser suite
TYPE: RELEASE
SIZE: SMALL
STATUS: COMPLETE
PRIORITY: P1
CREATED_AT: 2026-08-25
AFFECTED_MODULES: [apps/admin, e2e]
AGENTS: [Architect, QA, Integrator, Release/DevOps]
DEPENDENCIES: origin/develop 3d1e750c
CURRENT_PACKAGE:
COMPLETED_PACKAGES: [WP-01, WP-02]
BLOCKED_PACKAGES: []
OWNER_DECISIONS: 0
FINAL_STATUS: COMPLETE - merged as 08d79012 via PR #47. Vercel admin Ready in 40s; Render correctly skipped because no file under its services/api rootDir changed, so /api/health still reporting 2609275 is the right outcome rather than a stalled deploy.
---

# TASK-0021 — Release the tenant list identity fix and the first admin browser suite

## Objective

Promote `develop` to `main` so the tenant list shows a Name column again in
production. Finished when production serves the released commit and the tenant
list leads with the tenant's own name rather than its customer's.

Authorised by the owner. `main` is otherwise theirs to promote, and the
authorisation for [[TASK-0019]] was treated as spent rather than standing.

## What it changes

| Record | Production today | After |
|---|---|---|
| [[ITEM-0097]] | The tenant list leads with `Customer` and shows no tenant name — a saved preference removed the identity column | `Name` leads and cannot be hidden; `Legal name` available in the picker |

The saved preference itself is untouched. `essential` overrides it at render
time rather than rewriting anybody's stored state, so an operator's other column
choices survive.

## Work Packages

| WP_ID | TITLE | STATUS | DEPENDENCIES | AGENTS | BRANCH | SHA | QA_STATUS | BUGS | CI_STATUS | MERGE_STATUS |
|---|---|---|---|---|---|---|---|---|---|---|
| WP-01 | PR `develop` → `main`, exact-SHA CI verdict, merge | DONE | — | Integrator | develop | 53e57a6c | PASS | — | PASS | MERGED 08d79012 |
| WP-02 | Deployment outcome, production verification, release record | DONE | WP-01 | Release/DevOps, QA | main | 08d79012 | PASS | — | PASS | DEPLOYED |

## Risk

Smaller than [[TASK-0019]] and for the same reasons, restated because they are
what makes a same-day second release reasonable rather than reckless:

- **No schema change**, so no migration.
- **No environment or configuration change** — nothing under `packages/`,
  `render.yaml` or `turbo.json`.
- **Five files, all in `apps/admin` and `e2e`.** No API change at all, so the
  Render service is unaffected by anything but a redeploy of identical code.
- **The change is additive at render time.** `essential` forces a column
  visible; it removes nothing and writes nothing.

The surface this touches is the one an operator looks at, and it is now the only
surface in the repository with browser coverage.

## Pre-release validation

At `3d1e750c`, the exact commit being promoted.

| Check | Result |
|---|---|
| `api` | 222 suites, 1,764 tests |
| `admin` | 30 suites, **240** tests |
| `landing` | 11 suites, 149 tests |
| `web` | 23 suites, 449 tests |
| Typecheck — admin, e2e | clean |
| **Browser e2e** | **60 passed**, including all four Flow G scenarios |
| `CI required gate` | PASS (run 32786543763) |

**Flow G took three CI cycles to get right, and all three failures were the
suite's, not the product's.** No sign-in; then two assertions that would have
passed while the table was broken, both targeting the selection-checkbox column
by accident; then an anchored selector compared against an accessible name that
includes the resize handle's label.

Each was the same error the suite exists to catch — reasoning about the
definition instead of the rendered page — committed while writing the fix for
it. Recorded because a suite that cost three cycles on arrival is exactly what
`playwright.config.ts` warns trains people to ignore CI, and the answer to that
is to say so, not to quietly normalise it.

The product fix was green from the first push.

## Rollback

Revert the merge and redeploy. No migration, no configuration, no stored state
touched — an operator's saved column preferences are read, never written, by
this change.

## Repository Health

PRE_TASK_REPO_HEALTH — `MAIN_CHANGE_STATUS = UNTOUCHED` against `2609275`;
`UNEXPLAINED_DIRTY_FILES = 0`.

POST_TASK_REPO_HEALTH — recorded on the engineering history for SESSION-0049.

## History

- 2026-08-25 — created at `3d1e750c` on the owner's instruction to continue.

<!-- GRAPH:BEGIN — generated by scripts/rebuild-tasks.mjs; edit the record, not this block -->

## Related

- Records — [[ITEM-0097]]

<!-- GRAPH:END -->
